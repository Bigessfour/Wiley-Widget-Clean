# This file controls the behavior of Trunk: https://docs.trunk.io/cli
# To learn more about the format of this file, see https://docs.trunk.io/reference/trunk-yaml
version: 0.1
cli:
  version: 1.25.0
# Trunk provides extensibility via plugins. (https://docs.trunk.io/plugins)
plugins:
  sources:
    - id: trunk
      ref: v1.7.2
      uri: https://github.com/trunk-io/plugins
# Many linters and tools depend on runtimes - configure them here. (https://docs.trunk.io/runtimes)
runtimes:
  enabled:
    - go@1.21.0
    - node@22.16.0
    - python@3.10.8
# This is the section where you manage your linters. (https://docs.trunk.io/check/configuration)
lint:
  enabled:
    # Code formatting - essential for consistency
    - prettier@3.6.2
    # Security scanning - critical for production
    - trufflehog@3.90.5
    # .NET code quality and formatting
    - dotnet-format@8.0.0
    # PowerShell script analysis
    - psscriptanalyzer@1.24.0
    # Security vulnerability scanning
    - semgrep@1.133.0
    # License and dependency checking
    - osv-scanner@2.2.2
    # Git security scanning
    - gitleaks@8.28.0
  disabled:
    # Too aggressive on documentation formatting
    - markdownlint
    # Too strict on git whitespace in docs
    - git-diff-check
    # Too picky about GitHub Actions syntax variations
    - actionlint
    # Too picky about YAML formatting variations
    - yamllint
    # Security scanner with too many false positives on config files
    - checkov
  ignore:
    # Skip documentation formatting entirely
    - linters: [ALL]
      paths:
        - docs/**/*.md
        - "*.md"
        - README.md
        - CHANGELOG.md
        - CONTRIBUTING.md
    # Skip YAML formatting for GitHub workflows
    - linters: [prettier]
      paths:
        - .github/**/*.yml
        - .github/**/*.yaml
    # Skip binary and generated files
    - linters: [ALL]
      paths:
        - bin/
        - obj/
        - TestResults/
        - coverage/
        - "*.binlog"
        - "*.exe"
        - "*.dll"
actions:
  enabled:
    - trunk-announce
    - trunk-check-pre-push
    - trunk-fmt-pre-commit
    - trunk-upgrade-available
tools:
  enabled:
    - pwsh@7.4.1
    - converttosarif@1.0.0
