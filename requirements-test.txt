# Testing dependencies for Wiley Widget
pytest>=7.0.0
pytest-mock>=3.10.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0  # For parallel test execution
debugpy>=1.8.0  # For Python debugging support
watchgod>=0.8.0  # For file watching in development

# UI Testing dependencies
pywinauto>=0.6.8  # Windows UI automation
pytest-playwright>=0.4.0  # For web UI testing if needed
pillow>=10.0.0  # For screenshot comparison
opencv-python>=4.8.0  # For image processing in UI tests
psutil>=5.9.0  # For process monitoring in UI tests