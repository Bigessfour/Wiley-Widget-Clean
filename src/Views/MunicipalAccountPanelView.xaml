<UserControl x:Class="WileyWidget.Views.MunicipalAccountPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
             xmlns:syncfusionskin="clr-namespace:Syncfusion.SfSkinManager;assembly=Syncfusion.SfSkinManager.WPF"
             xmlns:notification="clr-namespace:Syncfusion.Windows.Controls.Notification;assembly=Syncfusion.SfBusyIndicator.WPF"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:models="clr-namespace:WileyWidget.Models;assembly=WileyWidget.Models"
             mc:Ignorable="d"
             d:DesignHeight="800"
             d:DesignWidth="1400">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <ObjectDataProvider x:Key="FundTypeValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:FundType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
        <ObjectDataProvider x:Key="AccountTypeValues" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="models:AccountType" />
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>
    </UserControl.Resources>

    <Grid Background="#FF0F1724" syncfusionskin:SfSkinManager.VisualStyle="FluentDark">
        <DockPanel>
            <syncfusion:Ribbon DockPanel.Dock="Top" x:Name="AccountsRibbon">
                <syncfusion:RibbonTab Caption="Accounts">
                    <syncfusion:RibbonBar Header="Data">
                        <syncfusion:RibbonButton Label="Load Accounts"
                                                  SizeForm="Large"
                                                  Command="{Binding LoadAccountsCommand}" />
                        <syncfusion:RibbonButton Label="Sync QuickBooks"
                                                  Command="{Binding SyncFromQuickBooksCommand}" />
                        <syncfusion:RibbonButton Label="Budget Analysis"
                                                  Command="{Binding LoadBudgetAnalysisCommand}" />
                        <syncfusion:RibbonButton Label="Clear Error"
                                                  Command="{Binding ClearErrorCommand}" />
                    </syncfusion:RibbonBar>
                    <syncfusion:RibbonBar Header="Filters">
                        <StackPanel Orientation="Vertical" Margin="4,4,8,0">
                            <TextBlock Text="Fund" Margin="0,0,0,4" />
                            <ComboBox ItemsSource="{Binding Source={StaticResource FundTypeValues}}"
                                      SelectedItem="{Binding SelectedFundFilter, Mode=TwoWay}"
                                      Width="160" />
                            <syncfusion:RibbonButton Label="Apply Fund Filter"
                                                      Margin="0,6,0,0"
                                                      Command="{Binding FilterByFundCommand}" />
                        </StackPanel>
                        <StackPanel Orientation="Vertical" Margin="8,4,4,0">
                            <TextBlock Text="Account Type" Margin="0,0,0,4" />
                            <ComboBox ItemsSource="{Binding Source={StaticResource AccountTypeValues}}"
                                      SelectedItem="{Binding SelectedTypeFilter, Mode=TwoWay}"
                                      Width="160" />
                            <syncfusion:RibbonButton Label="Apply Type Filter"
                                                      Margin="0,6,0,0"
                                                      Command="{Binding FilterByTypeCommand}" />
                        </StackPanel>
                    </syncfusion:RibbonBar>
                </syncfusion:RibbonTab>
            </syncfusion:Ribbon>

            <Grid Margin="0,8,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Border Grid.Row="0" Margin="12,0,12,8" Padding="12"
                        Background="#331A2C41" CornerRadius="6">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Status:" FontWeight="SemiBold" Foreground="#FFE0E6F0" />
                        <TextBlock Text="{Binding StatusMessage}" Margin="8,0,0,0" Foreground="#FFB9C8EC" />
                        <TextBlock Text="{Binding ErrorMessage}"
                                   Margin="12,0,0,0"
                                   Foreground="#FFF87171"
                                   Visibility="{Binding HasError, Converter={StaticResource BoolToVis}}" />
                    </StackPanel>
                </Border>

                <syncfusion:SfDataGrid Grid.Row="1" Margin="12"
                                       ItemsSource="{Binding MunicipalAccounts}"
                                       SelectedItem="{Binding SelectedAccount, Mode=TwoWay}"
                                       AutoGenerateColumns="False"
                                       AllowSorting="True"
                                       AllowFiltering="True"
                                       AllowGrouping="True"
                                       ColumnSizer="AutoWithLastColumnFill"
                                       RowHeight="30">
                    <syncfusion:SfDataGrid.Columns>
                        <syncfusion:GridTextColumn HeaderText="Account #" MappingName="AccountNumber" Width="140" />
                        <syncfusion:GridTextColumn HeaderText="Name" MappingName="DisplayName" Width="240" />
                        <syncfusion:GridTextColumn HeaderText="Fund" MappingName="FundDescription" Width="140" />
                        <syncfusion:GridTextColumn HeaderText="Account Type" MappingName="TypeDescription" Width="160" />
                        <syncfusion:GridNumericColumn HeaderText="Balance" MappingName="Balance" NumberDecimalDigits="2" Width="140" />
                        <syncfusion:GridTextColumn HeaderText="Notes" MappingName="Notes" Width="240" />
                    </syncfusion:SfDataGrid.Columns>
                </syncfusion:SfDataGrid>

                <Border Grid.Row="0" Grid.RowSpan="2" Background="#AA000000"
                        Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
                    <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                        <notification:SfBusyIndicator Width="80" Height="80"
                                                     AnimationType="DoubleCircle"
                                                     IsBusy="{Binding IsBusy}" />
                        <TextBlock Text="Processing municipal accounts..."
                                   Foreground="White"
                                   FontSize="14"
                                   Margin="0,12,0,0"
                                   HorizontalAlignment="Center" />
                    </StackPanel>
                </Border>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>
