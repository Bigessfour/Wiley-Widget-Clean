<Window x:Class="WileyWidget.ReportsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:syncfusion="http://schemas.syncfusion.com/wpf"
        mc:Ignorable="d"
        Title="Enterprise Reports"
        Height="800"
        Width="1200"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI">
    <syncfusion:SfSkinManager.Theme>
        <syncfusion:Theme ThemeName="FluentLight" />
    </syncfusion:SfSkinManager.Theme>

    <Grid Background="#FF0F1724">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Margin="16" Padding="16" Background="#661C283A" CornerRadius="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Orientation="Vertical">
                    <TextBlock Text="Start Date" Foreground="#FFE1F2FF" FontWeight="SemiBold" />
                    <syncfusion:SfDatePicker Value="{Binding StartDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="180" />
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Vertical">
                    <TextBlock Text="End Date" Foreground="#FFE1F2FF" FontWeight="SemiBold" />
                    <syncfusion:SfDatePicker Value="{Binding EndDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="180" />
                </StackPanel>

                <StackPanel Grid.Column="4" Orientation="Vertical">
                    <TextBlock Text="Enterprise" Foreground="#FFE1F2FF" FontWeight="SemiBold" />
                    <syncfusion:ComboBoxAdv ItemsSource="{Binding Enterprises}"
                                            DisplayMemberPath="Name"
                                            SelectedValuePath="Id"
                                            SelectedValue="{Binding EnterpriseId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                            Width="220"
                                            IsEditable="False" />
                </StackPanel>

                <StackPanel Grid.Column="6" Orientation="Vertical">
                    <TextBlock Text="Filter" Foreground="#FFE1F2FF" FontWeight="SemiBold" />
                    <TextBox Text="{Binding Filter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                             Width="220"
                             Margin="0,2,0,0" />
                </StackPanel>

                <StackPanel Grid.Column="8" Orientation="Horizontal" VerticalAlignment="Bottom">
                    <Button Content="Generate"
                            Command="{Binding GenerateReportCommand}"
                            Width="120"
                            Height="36"
                            ToolTip="Generate: Unleash the reports!"
                            Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}" />
                    <Button Content="Export PDF"
                            Command="{Binding ExportCommand}"
                            CommandParameter="pdf"
                            Width="110"
                            Height="36" />
                    <Button Content="Export Excel"
                            Command="{Binding ExportCommand}"
                            CommandParameter="excel"
                            Width="110"
                            Height="36" />
                </StackPanel>

                <StackPanel Grid.Column="10" VerticalAlignment="Bottom" Orientation="Horizontal">
                    <ProgressBar Width="140"
                                 Height="14"
                                 Minimum="0"
                                 Maximum="100"
                                 Value="{Binding ProgressPercentage}" />
                    <TextBlock Text="{Binding StatusMessage}" Foreground="#FFB6C8DE" FontSize="11" />
                </StackPanel>
            </Grid>
        </Border>

        <TabControl Grid.Row="1" Margin="16" SelectedIndex="0">
            <TabItem Header="Report Viewer">
                <Grid Background="#FF122034">
                    <TextBlock Text="Report Viewer not available - Syncfusion Reporting not installed"
                               Margin="16"
                               Foreground="White"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center" />
                </Grid>
            </TabItem>
            <TabItem Header="Tabular Data">
                <Grid Background="#FF122034">
                    <syncfusion:SfDataGrid ItemsSource="{Binding ReportItems}"
                                           Margin="16"
                                           AutoGenerateColumns="True"
                                           AllowSorting="True"
                                           AllowFiltering="True"
                                           ShowGroupDropArea="True"
                                           ColumnSizer="AutoWithLastColumnFill" />
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
